1.lezem docker is running


2.command to build image from a docker file :
>docker build -t app_name:latest .

3.when build is finished, check the image :
>docker images 

4.to create a container from that image, type :
>docker run -d -p 5000:5000 app_name:latest
note : in lfask code dont forget to add "host='0.0.0.0'"



/////////////

9.to visualize all containers (running or stooped) :
>docker ps -a (--all)

8.to run container (stopped one) :
>docker start container_ID

5.to visualize the page on chrome :
try http://127.0.0.1:5000/

7.to STOP contianer :
>docker stop container_ID

6.to visualize running containers (only running!!) :(bech t'thabet tsaker)
>docker ps

//////////////////////

10.to purge all containers(tfasa5 les containers lkol)
>docker container prune

11.open terminal inside a running container :
>docker exec nadapp:latest -it (bash ou /bin/bash)